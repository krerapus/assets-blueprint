# Local overrides for the example consumer (not managed by sync).
variables:
  forge: gitlab
  default_branch: main
  integration_branch: develop
  test_command: "uv run pytest"
  branch_prefix: "feature/"
overrides:
  - AGENTS.local.md
  - CLAUDE.local.md
  - .cursor/rules/key-principles.local.mdc
