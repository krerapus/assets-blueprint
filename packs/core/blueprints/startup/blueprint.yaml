name: startup
# Tracks package VERSION (./VERSION) — do not hardcode a semver here.
extends: default
description: Lightweight startup profile — planning harness plus PRD/ADR templates.

includes:
  templates:
    - prd.md
    - adr.md
    - review-checklist.md
  prompts:
    system:
      - agent-system.md

variables:
  forge: null
  default_branch: main
  integration_branch: main
